include:
  - project: 'common/gitlab-ci'
    ref: v0.4.15
    file: '/templates/npm-package.yml'

e2e_test:
  stage: test
  image: registry.kyso.io/docker/node-builder:16.15.1-alpine3.16
  before_script:
    - !reference [.npm-config, before_script]
  services:
    - docker:dind
  script:
    - apk add --no-cache docker-compose
    - docker-compose up -f ./test/docker-compose.yml --force-recreate &
    - npm install
    - npm run test
    